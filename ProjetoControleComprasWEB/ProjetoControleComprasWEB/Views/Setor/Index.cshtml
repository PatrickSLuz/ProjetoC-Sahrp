@model IEnumerable<Domain.Setor>

@{
    ViewData["Title"] = "Setores";
}

<h2>Gerenciar Setores</h2>

<p>
    <a class="btn btn-info" asp-action="Create">Novo Setor</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeSetor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DtCriacao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeSetor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DtCriacao)
            </td>
            <td>
                <a class="btn btn-outline-warning" asp-action="Edit" asp-route-id="@item.SetorId">Editar</a>
                <a class="btn btn-outline-danger" asp-action="Delete" asp-route-id="@item.SetorId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
